spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://54.180.167.253:5006/test?serverTimezone=Asia/Seoul&rewriteBatchedStatements=true   #rewriteBatchedStatements -> bulk insert 같은거 사용할 때 true설정
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    initialize-schema: never